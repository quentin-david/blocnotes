{% extends 'QTAppliBundle:Base:base.html.twig' %}

{% block contenu %}      
    <div class="w3-col m9">
        <h3>Suppression d'un topic </h3>
        <p>
            Etes vous sur de vouloir supprimer 'l'objet <b>{{ topic.titre }}</b>
        </p>
    </div>
    
    <div class="w3-col m9">
        <form action="{{ path('supprimer_topic', {'topic_num': topic.id}) }}" method="post">
            <a href="{{ path('lister_topic') }}" class="btn btn-default">
                Retour création topic
            </a>
            <input type="submit" value="Supprimer" class="btn btn-danger" />
        </form>
    </div>
{% endblock %}